package edu.sdccd.cisc191.template;

import java.util.Date;

public class Produce extends FoodItem {
    public Produce(String name, int quantity, Date expirationDate, String storageType) {
        super(name, quantity, expirationDate, storageType);
    }
}
